# 2026-08-27

- `compile` 指令：重建 wiki/sections.tsv。旧索引路径指向已不存在的子目录结构（specs/TS38.321_MAC/...），raw_sources 已扁平化为 specs/<file>.md。
- 新建 scripts/gen_section_index.py（此前 CLAUDE.md 引用但脚本不存在）：扫描标题生成 clause 索引，跳过 *_partNN.md 分片（与完整文件行数相加完全相等，是同一文件的切分）。file 列用 workspace 相对路径。
- 标题格式：`# 1Scope`（编号与标题无空格）、Annex 为 `###### Annex A (informative):Title` 六级标题、Annex 子节 `# A.1`。level = # 数量。
