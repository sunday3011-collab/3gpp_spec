# 2026-09-12

## 目录重排：specs/pdfs/word 封装进 3gpp_sources

- 新结构：`3gpp-specs/raw_sources/{3gpp_sources/{specs, pdfs, word}, product}`——规格类原文封装进大文件夹 `3gpp_sources/`，product 保持与其同级。
- 用 `git mv` 移动三个目录（14968 个文件，git 全部识别为 rename）。
- `config.py set` 更新 specs/pdfs/word 三个 Source 注册路径，`config.py` 内置默认 `DEFAULT_CONFIG` 同步。
- 全库引用批量替换（perl -pi）：CLAUDE.md、两份 README、SKILL.md、scripts/README.md、5 个脚本模板/注释、30 个 wiki/compiled 页面。**wiki/log.md 为 append-only 历史记录，未改动**。
- 重跑 `gen_section_index.py`：28025 章节 / 35 spec，路径指向新结构。
- **发现并修复**：`download_and_convert.py` 的 `PDF_OUT_DIR` 仍是硬编码旧路径（前一日 Edit 未生效/丢失），已改回 `cfg.source_path("pdfs")`。教训：改动后要用断言式冒烟测试验证每个路径变量，而非只看文件 diff。
- 用户级镜像已同步 download_and_convert.py / omml2latex.py / config.py。
- 环境坑：本机沙盒 shell 中 BSD `sed -i ''` 报空文件名错误，批量文本替换用 `perl -pi -e` 替代。

## 位置无关化改造（消除目录重组痛点）

- 根因：路径知识散落 5 处（sources.json 必要、sections.tsv 派生、compiled 页面/脚本模板/文档示例不该存）。
- compiled 页面 30 页"原始文档"行剥离路径文本，只留 `[[wikilink]]`（Obsidian 按 basename 解析，位置无关）。
- ingest_md_to_wiki.py / add_specs.py 概览页模板同步去掉路径尾巴。
- CLAUDE.md 新增「目录重组规约」：git mv → config.py set → gen_section_index.py 三步完成重组；硬性约定 compiled 页面不写路径、脚本走 config。
- SKILL.md 同步该规约。验证：compiled/ 目录 raw_sources 硬编码归零。

## 去壳：3gpp-specs/ 目录层移除

- 结构变更：`3gpp-specs/{wiki, raw_sources, personal_insights}` 全部 git mv 提到仓库根，3gpp-specs 壳删除；**仓库根即 Obsidian vault**。vault README（docs-wiki-README.md）内容与 CLAUDE.md/根 README 重复，git rm 删除。
- sources.json：wiki_root 设为 `.`，四个 Source 路径去掉 `3gpp-specs/` 前缀（如 `raw_sources/3gpp_sources/specs`）。
- **config.py 修复设计缺口**：`set wiki <路径>` 原先误写入 sources map 而非 wiki_root，已加特判路由到 cfg["wiki_root"]。镜像同步含此修复。
- sections.tsv 重建（file 列相对仓库根，不再有 3gpp-specs/ 前缀）；doctor 全绿；文档（CLAUDE.md/README/SKILL.md/scripts）路径基线全部改为仓库根。
- 根因价值：这轮重组只改了 sources.json + 文档 + 一次索引重建，compiled 页面因 wikilink 化**零改动**——位置无关化改造直接兑现收益。

## 仓库定位明确化（文档表述修正）

- 用户澄清定位：**本仓库是 Obsidian 知识库本体**——索引外部知识源（raw_sources 经 sources.json 注册，可仓库外），索引产出的 wiki 页面全部由本库管理。
- README.md 顶部改为三层机制表述：外部知识源只被索引引用 / wiki 页面（compiled·sections.tsv·authored）本库持有 / personal_insights 为内容层。
- CLAUDE.md「仓库定位与结构」同步；两处目录树注释改用"外部知识源（被索引对象）/ 索引产出的 wiki 页面（本库管理）"措辞。

## 代码源（code）接入设计

- 新增第三类外部知识源：**代码库**（仓库外独立 git 仓库，只索引不收纳；接入方式 = grep/find 直接检索，非外部 AI）。
- CLAUDE.md 新增「代码源（code）规约」章节：注册（`config.py set code-<名> <绝对路径>`，多仓库用后缀区分）、检索纪律（先蒸馏后源码/先地图后钻探/只读铁律）、引用格式（`仓库/路径:行号`，**不用 wikilink**——外部文件不在 vault，basename 解析不可达）、蒸馏页 `wiki/compiled/code_notes/<仓库名>/`。
- frontmatter 新增 code_notes 必填字段 `repo`/`commit`/`branch`——代码持续演进，页面是快照。
- 检索阶梯新增第 0 步"判源"：代码实现类问题走代码源规约，不进协议阶梯。
- 目录权限表新增外部代码库只读行；README 定位与 sources.json 注释同步；`wiki/compiled/code_notes/` 目录已建。
- config.py 开放 map 天然支持 code 类型，零代码改动。待用户提供仓库路径后 `set code` 注册。
- 教训：Edit 替换章节标题时会把它吃掉——用旧串=标题定位时，new_string 必须带回标题。

## CodeWiki 接入决策（代码知识生成器）

- 用户问代码知识生成能否用 CodeWiki。调研结论：两个同名工具——选 **FSoft-AI4Code/CodeWiki**（开源 CLI，静态依赖分析+LLM 层级分解，C/C++ 支持，arXiv:2510.24428）作为 code_notes 初稿生成器；**排除** gaosichun888/codewiki（Electron 应用自带知识库，会形成双真理源）。
- 接入纪律写入 CLAUDE.md「代码源规约」新增小节：产物不进 vault（留仓库旁）、策展=换格式（frontmatter/引用/交叉链接）、快照对齐（metadata.json commit hash + --update 增量）、grep/find 检索基线不变。
- 定位哲学：CodeWiki 产物是原料不是页面；它自动化的是"先地图后钻探"第一步。
