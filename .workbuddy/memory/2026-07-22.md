# 2026-07-22

## 协议大文件拆分
- 扫描 `3gpp-wiki/raw_sources/specs/`，对 >4MiB 的协议 md 文件做章节边界+字节均衡拆分
- 拆分 3 个文件，生成 10 个 part 文件，每个 ≤4MiB 且大小均衡：
  - `38.133_RRM_j40.md` (18.6M) → 6 parts (2.78~3.50M)
  - `38.331_RRC_j20.md` (4.8M) → 2 parts (2.41+2.41M)
  - `24.501_NAS_5GS_j62.md` (4.1M) → 2 parts (2.03+2.06M)
- 命名规则：`{原basename}_partNN.md`；原文件已保留
- 脚本逻辑（`/tmp/split_specs.py`）：按 3GPP 章节编号边界切；边界稀疏时（如 RRC 的 ASN.1 大段）退化为按行切，0.85~1.15 窗口内 snap 到最近边界

## specs 目录平铺
- 将 `3gpp-wiki/raw_sources/specs/` 下 35 个 `TSxxx/` 子目录内的协议文档平铺到 specs 根
- 删除 9 个 `.gitkeep` 占位符（平铺后无意义），移动 45 个 md 文件到根，删除所有空子目录
- 平铺后 specs 根 45 个 md 文件（含上一步的 10 个 part 文件），无子目录，无同名冲突
- 协议文档 basename 均含协议号前缀，天然无冲突

## 协议文件添加协议名标题
- 为 specs 根 45 个 md 文件开头插入一行协议名标题，格式 `# {协议号} {协议名}[(Part NN)]`
- 协议号/协议名从文件名解析：第一段=协议号，去掉末尾 `jXX` 版本与 `partNN`，中间段空格连接
- 示例：`38.133_RRM_j40.md` → `# 38.133 RRM`；`38.133_RRM_j40_part02.md` → `# 38.133 RRM (Part 02)`；`24.501_NAS_5GS_j62.md` → `# 24.501 NAS 5GS`
- 幂等判断：仅当首行已是目标标题才跳过；38.201 原有伪 `# 3GPP TS...` 标题，已在最前补 `# 38.201 PHY General`（形成双 H1，原内容保留）
- 脚本 `/tmp/add_titles.py`，支持 `--dry-run`

## 回退标题修改
- 应用户要求回退上一步标题插入：用精确逆操作移除每个文件开头的 `# {no} {name}[(Part NN)]\n\n` 前缀
- 45 个文件全部无损还原至加标题前的状态（含 38.201 恢复为原 `# 3GPP TS 38.201...` 伪标题）
- 脚本 `/tmp/revert_titles.py`，幂等（首行不匹配则跳过）

## 按 3M 阈值重新拆分
- 先删除上一轮（4M 阈值）生成的 10 个 part 文件（平铺后在 specs 根）
- 改 `/tmp/split_specs.py` 阈值 `MAX_BYTES = 3*1024*1024`，重新拆分
- specs 下 >3M 的文件仍为 3 个（38.133 18.6M / 38.331 4.8M / 24.501 4.1M；3.0M 的 38.101-1 实际 ≤3M 不拆）
- 生成 11 个 part 文件，全部 ≤3M（最大 2.9M）：
  - `38.133_RRM_j40.md` → 7 parts (2.32~2.94M)
  - `38.331_RRC_j20.md` → 2 parts (2.41+2.41M)
  - `24.501_NAS_5GS_j62.md` → 2 parts (2.03+2.06M)
- 命名沿用 `{原basename}_partNN.md`；原文件保留
