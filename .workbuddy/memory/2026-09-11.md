# 2026-09-11

## Source 位置可配置重构（sources.json + config.py）

- 新增仓库根 `sources.json`：Source 注册表（specs/pdfs/word/product），相对路径基于仓库根，支持仓库外绝对路径；类型是开放 map 可任意新增。
- 新增 `.claude/skills/3gpp-spec-downloader/scripts/config.py`：list/get/set/remove/doctor 子命令 + 库接口（`source_path()` / `wiki_root()`），其余脚本 `import config`。
- 重构 5 个脚本：download_and_convert / gen_section_index / convert_images / add_specs / ingest_md_to_wiki 全部改读配置，不再硬编码 `3gpp-specs/raw_sources/...`。
- 路径优先级：环境变量（OUT_MD_DIR/PDF_OUT_DIR/WIKI_DIR/SPECS_ROOT）> sources.json > 内置默认。`SOURCES_CONFIG` 可指定其他配置文件（仓库外运行用）。
- **意外修复**：重跑 gen_section_index 时发现旧 sections.tsv 的 file 列还带迁移前的前缀 `3gpp-wiki-v2/`（指向不存在路径），本次已修正为 `3gpp-specs/`。
- 用户级镜像已同步：download_and_convert.py / omml2latex.py / config.py（sources.json 不拷贝）。
- 文档同步：SKILL.md（新增 Source 配置章节）、scripts/README.md、CLAUDE.md、根 README.md。
