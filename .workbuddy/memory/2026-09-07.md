# 2026-09-07

## 项目级 skill 目录重构：scripts/ → .claude/skills/3gpp-spec-downloader

**背景**：用户要删仓库根 `scripts/`，将其收编为项目级 skill 工具流，供 Claude Code 与 WorkBuddy 双环境加载。

**决策**（用户确认）：实体放 `.claude/skills/`，`.workbuddy/skills/` 建相对 symlink 单内容双入口；scripts/ 立即迁移并删除。

**执行**：
- `git mv scripts → .claude/skills/3gpp-spec-downloader/scripts/`（git 识别 7 rename，历史可追溯）
- 5 个脚本（download_and_convert/convert_images/gen_section_index/ingest_md_to_wiki/add_specs）的 `REPO` 从硬编码"上溯2层"改为 `_repo_root()` 向上探测 `.git`，找不到回退上溯2层（兼容仓库外运行）
- 新增项目级 SKILL.md（仓库内工具总入口）+ `.claude/README.md`（双入口约定 + 变更史）
- symlink：`.workbuddy/skills/3gpp-spec-downloader → ../../.claude/skills/3gpp-spec-downloader`
- 更新用户级 skill `~/.workbuddy/skills/3gpp-spec-downloader`：源头指向改为项目级 skill，同步命令方向反转；脚本镜像已同步（diff 一致）
- 更新仓库根 README 与 3gpp-specs/CLAUDE.md 中的旧 scripts/ 引用

**关键教训**：对同一文件并行发起多个 Edit 会竞态丢改动（README 命令块曾部分丢失），同文件修改必须串行。

**新源头模型**：`.claude/skills/3gpp-spec-downloader/` = 仓库内唯一源头；用户级 = 仓库外镜像（单向拷贝 download_and_convert.py + omml2latex.py）；.workbuddy/skills = symlink 无需维护。命令代称 `SK=.claude/skills/3gpp-spec-downloader/scripts`。

**状态**：变更已 git add 暂存（3A/2M/7R），未 commit；`.workbuddy/memory/2026-08-28.md` 为用户遗留 untracked，未动。

## CLAUDE.md 上移仓库根（同日第二项）

**需求**：本目录作为 Claude Code 工程，`3gpp-specs/CLAUDE.md` 移至根目录并更新信息。

**执行**：
- `git mv 3gpp-specs/CLAUDE.md CLAUDE.md`（R 识别，历史保留）
- 重写为仓库级规约：前置「仓库定位与结构」「工具入口」两章（含 SK 代称、scripts 已迁入 .claude 的事实），原 178 行知识库治理规约完整保留为「知识库操作规约」章，用**路径基线声明**（本章相对路径以 `3gpp-specs/` 为基准）消解目录歧义，awk 示例改为全路径
- 修引用：根 README L24 → 指向根 CLAUDE.md；`3gpp-specs/README.md` L4 链接 → `../CLAUDE.md`
- 验证：原位置已空、无残留断链（wiki/log.md 历史记录除外）

**结构认知**：`3gpp-specs/` 现为纯知识库（vault），治理文档归仓库根。CLAUDE.md 路径基线模式值得复用。

## Commit + Rebase + Push（同日第三项）

- commit `9797232`（15 files，7 rename 全识别，symlink 以 mode 120000 记录）
- **fetch 发现远端领先 1 提交** `e777091`（他人/他机推送：Split large MD + 38.201 数据），同样改了 `scripts/download_and_convert.py`(+166)——与本地 rename+重构重叠
- `git rebase origin/main` **自动干净合并**：git 识别 rename 跟随，远端拆分功能（+166行）与本地 `_repo_root` 重构（区域分离）无缝共存；用 `diff <(git show e777091:旧路径) 新路径` 验证差异仅剩 _repo_root 的 17 行
- push `e777091..34025a5 main -> main`，本地与远端一致
- **经验**：他人改过自己正在 rename 的文件时，先 commit → rebase（3-way 自动跟随 rename），再 diff 远端版与合并版做内容保全验证，最后 push
