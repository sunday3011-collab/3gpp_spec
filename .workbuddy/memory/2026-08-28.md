# 2026-08-28

- git pull origin main：fast-forward c0ffdd6→7664dc7，拉入34份R19 PDF（V19.x）及大md分片（38.133拆15 part等），远端删除旧3gpp-wiki/wiki/compiled结构。
- `git mv 3gpp-wiki-v2 3gpp-specs` 重命名目录（14969文件，全部识别为rename），并同步更新 scripts/gen_section_index.py 与 scripts/download_and_convert.py 中的默认路径（WIKI_DIR、OUT_MD_DIR、PDF_OUT_DIR），已提交。
- 注意：wiki/log.md 中历史日志仍含旧名"3gpp-wiki-v2"，属历史记录未改动。远端同步需手动 push。
- 封装skill：更新既有 `~/.workbuddy/skills/3gpp-spec-downloader/`（原fetch_spec.py/pandoc方案已过时）——捆绑副本替换为仓库版 download_and_convert.py + omml2latex.py，重写SKILL.md（三种模式：docx转md / --pdf ETSI下载 / --split 2MB拆分；R19>R18回退；仓库scripts/为唯一源头，skill副本需env覆盖输出路径）。冒烟测试通过（38.101-5 PDF V19.5.0下载成功）。
- README更新：根README重写（3gpp-specs新目录名、35份ETSI PDF、2MB拆分、--pdf/--split/gen_section_index工作流、协议覆盖表加23.501/23.502/38.401）。同时修复scripts/*.py与scripts/README.md中残留的旧路径"3gpp-wiki"（上次sed只替换了"3gpp-wiki-v2"，漏了无后缀的旧引用，涉及ingest_md_to_wiki.py/add_specs.py/convert_images.py/gen_section_index.py）。已提交7bb588d。
